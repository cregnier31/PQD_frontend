import React from "react";
import './app.css';
import {Header} from '../header';

export const AppView = props => (
  <div className="App">
    <div className="App-header">
      <Header />
    </div>
  </div>
);